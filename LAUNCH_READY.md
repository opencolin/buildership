# BuilderShip — Launch Readiness Summary

> Generated by the launch-readiness orchestrator agent.
> Last update: 2026-06-01 (pre-launch). For full handoff context see
> `LAUNCH_PLAN.md`, `PUNCHLIST.md`, and `.launch/`.

## What shipped between Discovery and v1.0

### Council decisions executed (from `PUNCHLIST.md`)

- **Q3 (CTA mismatch):** Primary hero CTA relabeled "Show us your claws →" so the button names the actual action (scrolls to the apply section whose eyebrow is also "Show us your claws"). [page.tsx]
- **Q4 (deadline framing):** Hard June 12 cutoff kept; "no deadline drama" line replaced with "one clean deadline, no extension games." [page.tsx]
- **Q5 (stale PRD):** Shelved with a one-line banner at the top of `PRD.md`. Not reconciled.
- **Q7 (beluga caviar):** Cut from hero subhead. Cleaner alliteration retained ("building, bowling, beer, sunset cruise"). [page.tsx]

### Cohort change (user-driven, 40 → 30 finalists)

Swept across: homepage copy (hero, FAQ, stat card, perks, judging, "the bar" card), root layout metadata + OG + Twitter card, share-button text, OG image text, static event data (`lib/data.ts`), seed insert + onConflictDoUpdate. Office-hours capacities (Frontier Tower 40) left alone — different concept.

### Marketing / share readiness

- **OG image:** New `src/app/opengraph-image.tsx` (1200×630, navy + lime, "Hack the High Seas" headline, "30 builders. One yacht. $50K credits and a DGX Spark for the winner.", ship.builders + sponsor row). Auto-wires both `og:image` and `twitter:image` per Next 14 conventions. Renders cleanly in Satori (hotfix `display: flex` after `inline-flex` rejection).
- **Favicon:** `src/app/icon.svg` (copy of the existing BuilderShip mark from `/public/brand/buildership-mark.svg`). Next 14 auto-routes it.
- **Twitter handles:** `twitter.site` + `twitter.creator` = `@ship_builders` so shares attribute back to the account.

### Reliability / ops

- **Build decoupled:** `npm run build` is now just `next build`. The DB migrate + seed run via `npm run release` instead, which means a missing `DATABASE_URL` no longer fails the Vercel build. (Previously `build` = `migrate && seed && next build`.)
- **CI gate:** `.github/workflows/ci.yml` runs `typecheck` + `next build` on every PR/push to main. Catches breakage before Vercel Production sees it.
- **Auth slug bug fixed:** `src/server/auth/config.ts` was using `CODE_CRUISE_SLUG = "code-cruise"`, which doesn't exist in the seed (real slug is `"buildership"`). Auto-RSVP on signIn was silently failing for every login. Renamed to `BUILDERSHIP_SLUG = "buildership"`.

### Copy fixes

- Rubric typo "how working the working demo actually is" → "how working the demo actually is" [page.tsx:47]
- Off-brand mailto `events@agenthack.ai` → `collin@dabl.club` with prefilled subject [page.tsx:689]
- Dead `⌘K` search input on `/docs` removed (had no handler; chip nav directly below provides the same functionality) [docs/page.tsx]
- Duplicate `public/nebius-wordmark.svg` deleted (canonical is `/public/brand/nebius-wordmark.svg`, all refs point there)

### Verified independently (no code change needed)

- All 3 Luma URLs return HTTP 200: `luma.com/ship.builders`, `luma.com/clawcamp-human-tech`, `luma.com/clawcamp-5-18`
- TypeScript `tsc --noEmit` clean across all branches before merge
- OG image renders 198 KB PNG at 1200×630

## What's still pending before launch (3 Colin-side items)

### 1. Vercel Production env vars
Before triggering the next Vercel deploy, confirm the following are set in **Production scope** in the Vercel dashboard (under Settings → Environment Variables):

- `DATABASE_URL` (Neon connection string)
- `AUTH_SECRET` (32+ char random; `openssl rand -base64 32`)
- `AUTH_GITHUB_ID` (GitHub OAuth app client ID)
- `AUTH_GITHUB_SECRET` (GitHub OAuth app client secret)
- `NEXT_PUBLIC_APP_URL` = `https://ship.builders`
- Any other `NEXT_PUBLIC_*` keys the bundle reads (these are baked at build time — must exist when the build runs or they're permanently `undefined`)

If any of these are missing, the deploy will succeed but auth-touching pages will 500.

### 2. OpenClaw resolution
Council default is **demote OpenClaw to workshop partner** across all surfaces. This was held back from v0.1 because it's a sponsor-relationship decision worth your explicit call.

Two paths:
- **Demote (default):** I'll strip OpenClaw from the "Hosted by" hero row and the homepage `partners` array, and reconcile the partnersJson list in seed.ts.
- **Promote (override):** You confirm a signed co-host commitment and supply a wordmark SVG. I'll add OpenClaw to the hero row, the seed sponsors insert, and the eventSponsors link.

Tell me which and I'll execute in v0.2 within minutes.

### 3. `/sponsors-only` pricing intent
This page is `noindex` but not auth-gated. It currently exposes internal pricing/tiers to anyone with the URL.

- **OK as-is:** confirm pricing is intentionally public → keep route out of any shareable nav/OG, ship.
- **Needs gating:** I'll add basic auth (env-var-driven user/pass) or NextAuth-protected gating before launch.

### 4. Local GitHub-login dev environment
You hit "GitHub login is not working" earlier. Root cause: no `.env.local` in the fresh clone + `AUTH_SECRET` missing + GitHub OAuth credentials missing + no working `DATABASE_URL`. Production is unaffected (assuming the env vars get set per item 1). Local dev is a separate fix — let me know if you want me to scaffold it.

## Known issues / explicitly deferred (logged but not blocking)

- **Hero image weight:** `public/hero/lobster-yacht-bridge.png` is 2.1 MB. Will hurt mobile LCP. Deferred to v0.2 polish if time permits; tagged in `PUNCHLIST.md` as a Risk/Ops + Marketing item.
- **Hardcoded event schedule:** `/events/[slug]` renders the same agenda for every event. Cosmetic for tonight, structural for v2.0.
- **No on-site lead capture:** RSVPs go straight to Luma; we don't own the lead. v1.5 item.
- **No error monitoring:** runtime 500s are invisible until reported. v1.5 item (Sentry; env var already in schema).

## Release roadmap state

| Release | Status | Worktree / artifact |
|---|---|---|
| v0.1 critical | shipped to main | `main` history |
| v0.2 polish | shipped to main | `main` history |
| v0.5 content | shipped to main | `main` history |
| v1.0 launch tag | ready (pending 3 Colin items) | tag `v1.0` on main |
| v1.5 reactive | planned, worktree ready | `.worktrees/v1.5-reactive` + `.launch/v1.5-plan.md` |
| v2.0 structural | planned, worktree ready | `.worktrees/v2.0-structural` + `.launch/v2.0-plan.md` |

## Verification checklist for the launch tweet

Before the announcement tomorrow:

- [ ] Hit `https://ship.builders` from a clean browser, confirm:
  - [ ] Hero says "30 onboard"
  - [ ] CTA reads "Show us your claws →"
  - [ ] Apply card says "one clean deadline, no extension games"
  - [ ] Sponsor row shows Composio + Nebius + Tavily logos
  - [ ] Browser tab shows the BuilderShip mark favicon (not the generic globe)
- [ ] Paste `https://ship.builders` into iMessage, Slack, and a draft tweet — confirm the OG card renders ("Hack the High Seas." + 30 builders tagline)
- [ ] Run Twitter's [card validator](https://cards-dev.twitter.com/validator) on `https://ship.builders`
- [ ] Click the hero CTA → does it scroll to apply? Click "Register →" inside apply → does it land on `luma.com/ship.builders`?
- [ ] Confirm Vercel Production deploy is green and on the latest commit (`git log origin/main -1`)
